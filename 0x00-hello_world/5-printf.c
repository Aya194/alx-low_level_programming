#include <stdio.h>

/**
 * main - Function to print line of text using printf
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
